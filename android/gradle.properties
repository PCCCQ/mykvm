org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# This checkout lives under a non-ASCII path (E:\\ai生成程序\\mykvm). AGP's path
# check is a conservatism, not a hard limitation: the SDK/NDK/JDK all live under
# ASCII paths, so only the project directory itself is affected.
android.overridePathCheck=true
